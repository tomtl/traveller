class HotelsController < ApplicationController
  def search
  end

  def results
    region = params[:city]
    
    # region_id = get the region id from the API (region)
    # popular_hotels = get the popular hotes from the API using the region id (region_id)
  end
end
